# The URL the site will be built for
base_url = "https://www.jnakayama.com"

title = "Johannes Nakayama"
description = "Johannes Nakayama's personal website"

theme = "apollo"
taxonomies = [{ name = "tags" }]

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = false

[extra]
theme = "auto"
socials = [
  { name = "linkedin", url = "https://www.linkedin.com/in/johannes-nakayama/", icon = "linkedin" },
  { name = "github", url = "https://github.com/JohannesNakayama/", icon = "github" },
  { name = "gitlab", url = "https://gitlab.com/jnakayama/", icon = "gitlab" },
  { name = "bluesky", url = "https://jnakayama.bsky.social", icon = "bluesky" },
]
menu = [
    # { name = "/posts", url = "/posts", weight = 1 },
    { name = "/projects", url = "/projects", weight = 2 },
    { name = "/about", url = "/about", weight = 3 },
    { name = "/cv", url = "/cv", weight = 3 },
    { name = "/tags", url = "/tags", weight = 4 },
]
